# This file was generated by Apache CLI Tools
# 2016 (c) Denis Sedchenko [http://github.com/odin3]
# 2017 (c) Sergey Proezdov
# Configuration for host '%HOSTRING%'

<VirtualHost *:80>
    # Host URL
    ServerName www.%HOSTRING%
    ServerAlias %HOSTRING%
    ServerAdmin info@%HOSTRING%

    # Path to document root
    DocumentRoot %HOSTPATH%/hosts/%HOSTRING%/%HOSTRING%/web

    <Directory %HOSTPATH%/hosts/%HOSTRING%/%HOSTRING%/web>
        # Directory index
        DirectoryIndex index.html index.htm index.php
        # Allow .htaccess
        AllowOverride All
        # Access
        Order allow,deny
        Allow from all
        # New directive needed in Apache 2.4.3: 
        Require all granted

	# use mod_rewrite for pretty URL support
	RewriteEngine on
	# If a directory or a file exists, use the request directly
	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-d
	# Otherwise forward the request to index.php
	RewriteRule . index.php
    </Directory>

    # Error logs
    ErrorLog %HOSTPATH%/hosts/%HOSTRING%/error.log
    CustomLog %HOSTPATH%/hosts/%HOSTRING%/requests.log combined
</VirtualHost>


